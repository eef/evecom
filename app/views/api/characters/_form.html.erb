<%= form_for(@api_character) do |f| %>
  <% if @api_character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_character.errors.count, "error") %> prohibited this api_character from being saved:</h2>

      <ul>
      <% @api_character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
